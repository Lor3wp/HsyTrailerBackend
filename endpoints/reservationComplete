// reservationComplete.js complete reservation and delete temp reser
const express = require("express");
const router = express.Router();

router.get("/", (req, res) => {
  res.send("This is endpoint 2");
});

module.exports = router;
